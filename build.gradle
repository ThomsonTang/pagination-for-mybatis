apply plugin: 'java'

repositories {
    mavenLocal()
}

dependencies {
    compile group: 'commons-collecions', name: 'commons-collecions', version: '3.2'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}
